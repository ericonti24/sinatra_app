<h1>Welcome <%= @user.username %> to your Sports Join account</h1>
  <p>To register for a sport you can click on the "Register" button below. The sport(s) you are registered for will show below after you have registered.</p>
  <br><br>
  <a href="/register">Register</a>
</form>
<h2>Joined Sport(s)</h2>
<% @user.sports.each do |s| %>
  <p><%= s.sport %> - 
    <% if current_user == s.user %>
      <a href="/sports/<%= s.id %>/edit">Edit</a> - 
      <form action="/sports/<%= s.id%>" method="POST">
        <input type="hidden" name="_method" value="delete">
        <input type="submit" value="Delete">
      </form>
    <% end %>
  </p>
<% end %>
    






